# PostgREST 配置文件

# 数据库连接配置
db-uri = "postgres://aki:20138990398QGL@gmailcom@pgm-wz9z6i202l2p25wvco.pg.rds.aliyuncs.com:5432/postgres"
db-schemas = "public"
db-anon-role = "web_anon"
db-pool = 10
db-pool-timeout = 10

# API 配置
server-host = "0.0.0.0"
server-port = 3001

# JWT 配置（可选，用于认证）
# jwt-secret = "your-secret-key-here"
# jwt-aud = "your-audience"

# 日志配置
log-level = "info"

# OpenAPI 配置
openapi-mode = "follow-privileges"
openapi-server-proxy-uri = ""
